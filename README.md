# Strømming-eye

Requires npm & npx

First time setup:
```
npm install
```

To run just:
```
npm run dev
```

And to build tailwindcss from app.css use:

```
npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch
```

